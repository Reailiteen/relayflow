{
  "extends": "@relayflow/config/tsconfig/library.json",
  "compilerOptions": {
    // crypto.randomUUID() — available on Node, browsers and React Native.
    "lib": ["ES2023", "DOM"]
  },
  "include": ["src/**/*.ts"]
}
