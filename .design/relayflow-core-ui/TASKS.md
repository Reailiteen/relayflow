# Build Tasks: RelayFlow Core Workflow UI

Generated from: `.design/relayflow-core-ui/DESIGN_BRIEF.md`
Date: 2026-08-01

## Foundation

- [ ] **Create the no-backdrop side-panel system**: Add a dedicated right-side `SidePanel` primitive with no overlay element, responsive full-width behavior on small screens, sticky header/footer, scrollable body, focus management, Escape/close support, accessible title/description, and an optional unsaved-change guard. Do not change centered confirmation dialogs or the mobile navigation drawer in this slice. _Creates: `SidePanel`, `SidePanelTrigger`, `SidePanelContent`, `SidePanelHeader`, `SidePanelBody`, `SidePanelFooter`; reuses Radix Dialog primitives, existing tokens, `Button`, and `cn`._
- [ ] **Create shared workflow form feedback**: Standardize pending submission, field issues, form errors, success notices, dirty-state confirmation, reason capture, and action-result mapping so every panel behaves consistently and retains values on failure. _Creates: `WorkflowForm`, `FormErrorSummary`, `ActionNotice`, `ReasonField`, and a small action-result hook; reuses `TextField`, `TextAreaField`, `Button`, and status tokens._
- [ ] **Split the monolithic cycle workspace into stage modules**: Keep the existing shell, cycle switcher, gate strip, metrics, and activity timeline, but move each workspace into a focused server/client boundary so panels can load only the records and actions they need. _Modifies: `cycle-workspace.tsx` and `render-cycle-workspace.tsx`; creates stage-specific allocation, positions, selection, recovery, and placements modules; reuses `Panel`, `MetricBar`, `Badge`, and canonical cycle routes._
- [ ] **Complete the cycle-scoped action/read-model façade**: Add the thin, authorized use-case and server-action entry points still missing for UI operations—participation saves, task lifecycle, placement confirmation, fallback actions, requirement templates/amendments/submissions/reviews/waivers, signatures, redistribution responses/grants/closure, and cycle-scoped interview transitions. Every input includes `cycleId` and every read model is privacy-filtered. _Modifies existing logic and server actions; reuses ports and fixture behavior rather than duplicating rules in React._
- [ ] **Add reusable operational detail patterns**: Build compact history, state-transition, privacy-safe checklist-progress, affected-hours, and immutable revision components used across side panels. _Creates shared `HistoryList`, `TransitionRail`, `ChecklistProgress`, `HoursImpact`, and `RevisionList`; reuses `ActivityTimeline`, `Badge`, rows, panels, and existing tokens._

## Phase 1 — Cycle Setup and Allocation

- [ ] **Replace cycle creation with a working side-panel flow**: Open from the cycle list without layout shift; capture name, dates, funded hours, selection mode, all deadlines, and optional clone source; conditionally show offer-window inputs; validate chronological order with Qatar-local date/time handling; preview clone scope; preserve entries on errors; prevent double submission; and route to the new cycle only after success. _Modifies `CreateCycleForm`; uses the new `SidePanel` and workflow form feedback; reuses `createCycleAction` and `listCycles`._
- [ ] **Add cycle edit, clone, and archive actions to the cycle list**: Open create/edit/view panels from each row, show lifecycle and budget context, reuse the creation fields for safe edits, provide clone-as-new prefill, and archive through a separate reasoned confirmation with archived/read-only presentation. _Modifies the cycles page; reuses `updateCycleAction`, `archiveCycleAction`, cycle cards, and the cycle form panel._
- [ ] **Build QSTP participation intake and startup participation views**: Provide a QSTP side panel to invite/edit one startup's participation, request, disciplines, score, internal notes, and published justification; provide startup read/response states with internal fields excluded. Cover declined, suspended, archived, zero-hour, and empty-cycle states. _Creates participation table/row/panel; reuses `saveParticipation`, startup affiliations, badges, and activity history._
- [ ] **Build prioritization review and proposal adjustment**: Show ranked proposals, budget utilization, tier deltas, request ceilings, internal rationale, version history, superseded runs, and a side panel for manual tier adjustments with live budget impact and required override reason. Publication remains a deliberate cycle-wide action. _Modifies allocation workspace; creates prioritization review table and adjustment panel; reuses `runPrioritization`, allocation revisions, `HoursImpact`, and activity events._
- [ ] **Complete allocation publication and acknowledgement**: Add a publication review panel summarizing startup-facing decisions, over-budget blockers, unpublished changes, and resulting revision; show startups only their published tier/justification and a clear acknowledgement action; reflect acknowledgement in stage gates and position availability. _Modifies allocation workspace and actions; reuses `publishAllocationsAction`, `acknowledgeAllocationAction`, stage gates, and revision history._
- [ ] **Make stage advancement a real gate-review panel**: Replace the generic reason with a panel listing each blocking check and warning, affected records, navigation links to fix blockers, and a mandatory operator-entered reason for warning overrides. _Modifies `AdvanceStageAction`; creates gate-review panel; reuses `advanceCycleStage`, `StageGateChecklist`, and activity history._

## Phase 2 — Position Collection and Approval

- [ ] **Move startup position creation into a cycle-scoped side panel**: Replace the legacy full-page form with a canonical panel covering work arrangement, requirements, supervisor details, seats, hours, duration, accelerated-round context, and live derived capacity. Support save draft and submit as separate actions, with allocation acknowledgement, deadline, extension, suspension, zero-hour, and over-capacity failures rendered inline. _Modifies the startup positions workspace and form; reuses position entities, `submitPosition`, transition logic, allocation totals, and `HoursImpact`._
- [ ] **Add position edit, resubmit, withdraw, and read-only detail panels**: Allow owners to edit drafts and requested corrections, show the complete feedback/revision history, preserve locked/filled/closed records as read-only, and surface effective deadlines and approved exceptions. _Creates position detail/editor panel; reuses `transitionPosition`, review history, exception records, and `HistoryList`._
- [ ] **Complete QSTP position review in table and board views**: Open one review panel from either view for under-review, approve, request changes, reject/close, lock, reopen-with-reason, and reapproval. Display capacity, allocation acknowledgement, deadline status, round source, supervisor, and readiness invalidation warnings. _Modifies QSTP position table/board/review controls; reuses existing views, `transitionPosition`, `DecisionDialog` concepts, and activity events._
- [ ] **Polish position state coverage**: Add intentional empty, late, over-capacity, changes-requested, resubmitted, approved, locked, filled, withdrawn, and permission-read-only presentations with consistent calls to action. _Modifies both position workspaces; reuses status badges, panels, and shared action notices._

## Phase 3 — Candidate Handoff, Assessment, and Selection

- [ ] **Cycle-scope candidate import and pool assignment**: Move import and pool-sharing into no-backdrop side panels, show parsed-row preview and duplicate report, allow multi-position assignment, display visibility timestamps, and prevent assignment to unapproved positions. _Modifies candidate admin actions and board/table; reuses import/share logic, `SidePanel`, and existing candidate rows._
- [ ] **Complete candidate availability and withdrawal UI**: Give candidates a cycle-specific availability panel, clear consequence copy, withdrawal confirmation, and propagation status across pools; show startups unavailable/withdrawn candidates without leaking hidden records. _Modifies candidate availability/journey and startup pool views; reuses availability use cases and lifecycle badges._
- [ ] **Complete interview workflows for all actors**: Build a side-panel timeline supporting request, candidate confirm/decline, schedule, reschedule, completion, no-show, feedback, recording/transcript simulation, and second interview. Restrict assessment actions to owner/member/supervisor roles and keep selection controls unavailable to supervisors. _Modifies interview pages and client workspace; creates interview detail/action panel; reuses existing scheduling/feedback logic and adds only missing cycle-scoped transition façades._
- [ ] **Complete reusable task-template and assignment UI**: Let startups manage position templates and assign multiple tasks, let candidates submit simulated files/links only for their own assignments, let authorized startup reviewers record accountable notes and late acceptance, and give QSTP full read access. _Creates template list/editor, assignment panel, candidate task form, and review panel; reuses task ports, `SimulatedFile`, revision/history patterns, and role capabilities._
- [ ] **Build the startup candidate process panel**: From table or board, open a unified candidate-position history showing availability, interviews, tasks, feedback, offer/selection eligibility, seat capacity, and the next authorized action. First-come selection reserves atomically; candidate-choice creates non-blocking offers. _Modifies startup candidate board/pool; creates process detail panel; reuses selection, interview, and task records._
- [ ] **Complete candidate offer and reservation responses**: Show first-come reservations and candidate-choice offers in one cycle-scoped inbox with deadline, position/startup context, accept/decline actions, stale/conflict failures, sibling-offer resolution, and preserved history. _Modifies candidate offers workspace; reuses `respondToSelection`, `acceptOffer`, selection statuses, and action notices._
- [ ] **Build QSTP placement-confirmation panel**: For accepted selections, collect final dates and supervisor, show candidate/seat/hour checks, and call atomic confirmation that creates the Stage 5 placement. Final-seat confirmation visibly withdraws remaining offers while retaining history. _Creates confirmation panel and queue; reuses `confirmPlacement`, `HoursImpact`, placement records, and selection history._
- [ ] **Complete conflict resolution UI**: Replace the current narrow conflict dialog with a side panel showing previous/new startup, candidate impact, seat/hour impact, both selection histories, dismiss, and atomic override with a mandatory reason and high-risk confirmation where applicable. _Modifies QSTP conflict view; reuses `resolveConflict`, `SelectionConflict`, activity events, and shared decision patterns._
- [ ] **Complete candidate-choice fallback UI**: Add QSTP controls to open an expired-offer fallback, choose/default the 48-hour deadline, accept/decline/expire each step, advance to the next offer, and perform manager-only candidate-choice override through a distinct high-risk confirmation. _Modifies selection workspace; creates fallback detail panel; reuses `openCandidateChoiceFallback`, `respondCandidateChoiceFallback`, and `overrideCandidateChoice`._
- [ ] **Unify exception request and decision UI**: Move startup position/selection extension requests and QSTP review into side panels; show effective deadline, request timing eligibility, affected hours, approve/reject notes, and approved protection throughout downstream screens. _Modifies startup exception and QSTP exception pages; reuses exception use cases, `HoursImpact`, and activity history._

## Phase 4 — Recovery, Replacement, and Redistribution

- [ ] **Build complete recovery-case review panels**: Show allocation, active committed placements, derived recoverable hours, exception/replacement protection, and explicit confirm/protect/close decisions. Loading the page must remain read-only. _Modifies recovery workspace; creates recovery detail panel; reuses recovery calculations, `confirmRecovery`, and activity history._
- [ ] **Build placement cancellation and replacement flow**: Add a reasoned cancellation side panel with candidate/hour/onboarding impact, preserve the cancelled record read-only, then expose protect-for-replacement or send-to-redistribution choices and a link to create a new replacement selection. _Modifies placement and recovery workspaces; reuses `cancelPlacement`, recovery cases, replacement links, and immutable history._
- [ ] **Build redistribution round creation and invitation panels**: Select eligible confirmed recovery cases, set accelerated deadlines, preview total available hours, create the next numbered round, rank eligible startups, issue tiered invitations, and show accept/decline/expiry states. _Modifies recovery workspace; creates round builder and invitation panels; reuses `createRedistributionRound`, `inviteRedistribution`, participation scores, and hour tiers._
- [ ] **Complete grant, accelerated progress, and round closure UI**: Let invited startups respond, create linked allocation revisions on acceptance, route accepted startups into round-scoped position/selection work, show remaining hours, and support close/cancel plus another round when hours remain. _Creates round detail/progress panels and startup invitation view; reuses redistribution ports, allocation revisions, canonical position/selection modules, and stage status badges._

## Phase 5 — Documentation and Ready to Start

- [ ] **Build cycle requirement-template management**: Let QSTP create, edit, deactivate, and inspect candidate/QSTP/startup requirement templates; let startups add position-specific requirements before confirmation; make snapshot timing and future-placement scope explicit. _Creates requirement-template table and editor panel; reuses requirement ports, cycle/position records, and shared form feedback._
- [ ] **Build role-safe placement checklist views**: Show every actor the status and ownership of relevant checklist items while exposing submission contents only to the candidate and QSTP. Startup views may show candidate-item completion but never ID/bank fields, storage paths, or previews. _Modifies placement workspace and read model; reuses `RequirementChecklist`, privacy filtering, and badges._
- [ ] **Complete simulated submission and correction workflow**: Add candidate/startup upload panels with metadata-only simulated files, revision history, extracted-versus-confirmed fields, correction reasons, resubmission, and clear requested/awaiting/uploaded/review states. _Creates requirement submission/editor panels; reuses requirement submission ports, `SimulatedFile`, and `RevisionList`._
- [ ] **Complete QSTP verification and checklist amendment UI**: Build verification panels for approve, reject, request correction, waive, expire, and amend-existing-checklist-with-reason, retaining all revisions and activity history. _Modifies QSTP verification queue; creates requirement review/amend panels; reuses requirement ports and decision patterns._
- [ ] **Build explicit simulated agreement signing**: Require the signer to open the fixture document, accept the declaration, and submit identity/timestamp for QSTP and startup agreements; show signed metadata and prevent duplicate or unauthorized signing. _Creates agreement viewer/signing panel; reuses signature ports and activity timeline._
- [ ] **Complete readiness and onboarding review**: Replace blind readiness buttons with role-specific panels showing every blocker, candidate/startup confirmations, final dates/hours/supervisor, signatures, unresolved conflict/exception status, QSTP final approval, Ready to Start, onboard, cancelled read-only, and audit history. _Modifies placement cards/actions; creates readiness review panel; reuses `placementReadinessBlockers`, readiness/finalization actions, and checklist progress._

## Cross-Cutting States, Responsive Behavior, and Accessibility

- [ ] **Make all mutation controls role- and state-correct**: Derive visible controls from server-provided permissions/state, verify viewer and supervisor restrictions, and render denial/stale-state responses without optimistic corruption. _Modifies all workflow modules; reuses access capabilities and server authorization._
- [ ] **Add deep-linkable panel navigation**: Preserve `cycleId`, workspace, filters, table/board mode, and selected record when opening panels; closing returns focus to the trigger and browser Back closes the panel before leaving the workspace. _Modifies canonical route/query handling; reuses Next App Router and side-panel primitives._
- [ ] **Polish responsive behavior**: Keep dense tables/boards on desktop, use stacked summaries on narrow screens, make panels full-width on mobile, keep primary actions reachable in sticky footers, and prevent long names, notes, and translated text from overflowing. _Modifies all new panels and workspace modules; reuses responsive tokens and existing console shell._
- [ ] **Complete accessibility pass**: Verify keyboard-only operation, focus return, visible focus, labelled errors, live status announcements, semantic tables/timelines, disabled-state explanations, reduced motion, and no dependence on color alone. _Modifies shared primitives and every workflow panel; reuses Radix accessibility behavior and existing focus tokens._

## Verification and Release Gates

- [ ] **Add focused UI tests per vertical slice**: Test cycle form validation and routing, panel open/close/dirty behavior, conditional fields, role-specific control visibility, error retention, privacy-safe checklist rendering, and immutable history presentation. _Creates colocated Vitest/component tests using existing test conventions; modifies fixtures only to add deterministic scenarios where required._
- [ ] **Add fixture-backed Playwright journeys**: Cover QSTP cycle creation/edit through allocation, startup acknowledgement through position submission, candidate pool/interview/task/offer flows, conflict/fallback, recovery with one and repeated rounds, cancellation/replacement, document correction/signing/readiness/onboarding, cycle switching, direct URL isolation, and no-backdrop side panels. _Creates Playwright configuration, persona helpers, and smoke specs; reuses development fixture reset/scenario selector._
- [ ] **Run phase gates and final design review**: After each numbered phase run focused tests, `pnpm verify`, and the relevant browser journeys; at completion run the full suite and a visual review against this brief at desktop and mobile widths. _Reuses repository scripts and the design-review workflow; no feature is complete while its failure/read-only states are unverified._

